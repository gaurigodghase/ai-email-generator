I have implemented the solution as a Streamlit application and the code pertaining to that can be found in app.py, pages/email_details_page.py, pages/main_page.py, email_fetcher.py and reply_generator.py.

The jupyter notebook gives a prototype of the expectations from the llm. I built the application by testing on the prototype but the two implementatins are quite different.

